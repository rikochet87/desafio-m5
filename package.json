{
  "name": "desafio-m5",
  "version": "1.0.0",
  "license": "MIT",
  "description": "",
  "scripts": {
    "dev": "parcel serve ./src/index.html",
    "build": "parcel build ./src/index.html --dist-dir build --public-url /desafio-m5",
    "deploy": "gh-pages -d build",
    "build-dep": "npm run build && npm run deploy"
  },
  "devDependencies": {
    "gh-pages": "^3.2.3",
    "parcel": "^2.4.1"
  }
}
